---

openapi: 3.0.0
info:
  title: "Credit API"
  version: "1.0.1"

servers:
  - url: "/"

components:
  schemas:
    Record:
      type: "object"
      required:
        - user_id
        - rollercoaster_id
      properties:
        user_id:
          type: "integer"
        rollercoaster_id:
          type: "integer"
  parameters:
    record_id:
      name: "record_id"
      in: path
      required: True
      schema:
        type: "integer"
    user_id:
      name: "user_id"
      in: path
      required: True
      schema:
        type: "integer"

paths:
  "/":
    get:
      operationId: "record.main"
      responses:
        "200":
          description: ""
  /getAll:
    get:
      operationId: "record.get_all"
      responses:
        "200":
          description: ""
  /getById/{record_id}:
    get:
      operationId: "record.get_by_id"
      parameters:
        - $ref: "#/components/parameters/record_id"
      responses:
        "200":
          description: ""
        "404":
          description: "not found"
  /get/{user_id}:
    get:
      operationId: "record.get"
      parameters:
        - $ref: "#/components/parameters/user_id"
      responses:
        "200":
          description: ""
        "404":
          description: "not found"
  /submit:
    post:
      operationId: "record.submit"
      requestBody:
          description: ""
          required: True
          content:
            application/json:
              schema:
                x-body-name: "credit"
                $ref: "#/components/schemas/Record"
      responses:
        "201":
          description: ""